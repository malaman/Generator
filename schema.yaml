Category:
    fields:
        title: varchar(50)
    relations:
        Article: many

Tag:
    fields:
      name: varchar(30)
    relations:
      Article: many

Article:
    fields:
        title: varchar(50)
        text: text
    relations:
        Tag: many
        Category: one

